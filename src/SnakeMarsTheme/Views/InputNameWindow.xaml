<Window x:Class="SnakeMarsTheme.Views.InputNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nombre del Tema" Height="180" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#2D2D30"
        BorderBrush="#3E3E42"
        BorderThickness="1">
    
    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nombre para la instalación:" Foreground="White" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
        
        <TextBox x:Name="NameTextBox" Grid.Row="1" 
                 Height="30" FontSize="14" VerticalContentAlignment="Center"
                 Padding="5,0" Background="#1E1E1E" Foreground="White" BorderThickness="0"/>
        
        <TextBlock Text="Este nombre aparecerá en el menú de la aplicación." 
                   Grid.Row="2" Foreground="#AAAAAA" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancelar" IsCancel="True" Click="CancelButton_Click" Style="{StaticResource DialogButton}"/>
            <Button Content="Instalar" IsDefault="True" Click="OkButton_Click" Style="{StaticResource DialogButton}" Background="#007ACC"/>
        </StackPanel>
    </Grid>
</Window>
